<keymap>
  <global>
    <keyboard>
      <z>XBMC.ActivateWindow(VideoLibrary, MovieTitles)</z>
      <!-- Yellow (My TV)-->
      <e mod="ctrl"></e>
      <!-- Blue(My Music) 
      <i mod="ctrl">ActivateWindow(Videos,addons://sources/video/)</i>-->
      <l>ActivateWindow(Videos, Addons)</l>
      <!-- Info(Recorded TV) -->
      <k>XBMC.ActivateWindow(MusicLibrary, Artists)</k>
      <!--  (E)-->
      <g>XBMC.ActivateWindow(Videos, TVShowTitles)</g>
      <!-- Green (My Pictures) -->
      <c>ContextMenu</c>
      <!-- Menu (Guide)-->
      <!-- <asterisk>ActivateWindow(Programs)</asterisk>-->
	  
      <!-- Info (Live TV) -->
      <u>togglefullscreen</u>
      <!-- Red (My Videos) -->
      <t>XBMC.ActivateWindow(Favourites)</t>
      <!-- Record -->
    </keyboard>
  </global>
  <home>
    <keyboard>
    </keyboard>
  </home>
  <fullscreenvideo>
    <keyboard>
      <backspace>smallstepback</backspace>
      <k>showsubtitles</k>
	  <z>AspectRatio</z>
      <g>showtime</g>
	  <v>rewind</v>
	  <d>pause</d>
	  <a>SkipNext</a>
	  <o>SkipPrevious</o>
    </keyboard>
  </fullscreenvideo>
   <FullscreenLiveTV>
	<keyboard>
	<left>StepBack</left>
      <right>StepForward</right>
      <up>BigStepForward</up>
      <down>BigStepBack</down>
	  <t>ActivateWindow(TVRecordings,pvr://recordings/Default) </t>
	  <k>XBMC.ActivateWindowAndFocus(MyPVR, 31,0, 10,0)</k>
	 
    </keyboard>
  </FullscreenLiveTV>
</keymap>
